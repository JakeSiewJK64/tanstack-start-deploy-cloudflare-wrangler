# Description

this tanstack start project uses latest react and tanstack start versions as of writing. there are 2 endpoints:

- root
- `get-cookies` demonstrates how to retrieve and set ATK token cookie values necessary for authenticated API calls.